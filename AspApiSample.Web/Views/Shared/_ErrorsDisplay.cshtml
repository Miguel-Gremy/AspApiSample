@model ModelBase

@if (Model.Errors is not null)
{
    <div class="alert alert-danger" role="alert">
        @foreach (var error in Model.Errors)
        {
            <p>@error</p>
        }
    </div>
}